{
  "name": "pushup-web",
  "version": "0.1.0",
  "type": "module",
  "description": "PosePoint — web push-up form counter (MediaPipe Tasks Web + PWA)",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "node server.js"
  },
  "devDependencies": {
    "puppeteer-core": "^25.11.0"
  }
}
